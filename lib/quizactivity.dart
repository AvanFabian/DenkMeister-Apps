import 'package:flutter/material.dart';

class Quizactivity extends StatelessWidget {
  const Quizactivity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Quizactivity')),
      body: Center(child: Text('Quizactivity Page')),
    );
  }
}
